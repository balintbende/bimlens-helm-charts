apiVersion: v1
kind: ConfigMap
metadata:
  name: bimlens-{{ .Release.Namespace }}-common-configmap
data:

  RELEASE: "bimlens-{{ .Release.Namespace }}"

  HOST: {{ .Values.host.cloud }}
  CDN_HOST: {{ .Values.host.cdn }}
  API_HOST: {{ .Values.host.api }}
